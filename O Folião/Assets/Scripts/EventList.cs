using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EventList : MonoBehaviour
{
    [SerializeField] private Events[] normal;

    [SerializeField] private Events[] store;

    [SerializeField] private Events[] bar;

    [SerializeField] private Events[] extra;

    //public Events[] GetCertainEventType(Node.Type nodetype)
    //{
        //if(nodetype == Node.Type.Normal)
    //}
}

